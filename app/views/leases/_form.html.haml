= simple_form_for(lease) do |f|
  = f.error_notification
  = f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present?

  .form-inputs.grid.grid-cols-1.md:grid-cols-2.gap-4
    = f.association :property
    = f.association :tenant

    = f.input :start_date, as: :date, html5: true
    = f.input :duration_months

    = f.input :rent_amount
    = f.input :security_deposit_in_months

    = f.input :enhancement_period_months
    = f.input :enhancement_percentage
    = f.input :enhancement_fixed_amount

    = f.input :terminated_on, as: :date, html5: true, wrapper_class: "col-span-2 form-control", input_html: { class: "input input-bordered w-full" }

  .form-actions.mt-5.card-actions.justify-end
    = f.button :submit, class: "btn btn-primary"
    = link_to "Back", leases_path, class: "btn btn-ghost ml-4"
